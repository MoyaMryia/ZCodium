# 默认使用线上接口；本地地址覆盖写入未跟踪的 .env 或 .env.local。

# image-search 插件默认指向本地 loopback 后端（见
# apps/zcode-cli/packages/image-search-plugin/.zcode-plugin/plugin.json 的
# userConfig.imageSearchBaseUrl）。官方 MCP 鉴权的 origin 信任只放行两类目标：
# 与 ZCODE API origin 精确一致，或在此登记的 http loopback。不登记的话，
# 指向本地的请求会在客户端被 isOfficialMcpOriginTrusted 直接拒绝。
# 改 userConfig 默认端口时同步修改这里；逗号分隔可配多个。
ZCODE_OFFICIAL_MCP_DEV_TRUSTED_ORIGINS=http://127.0.0.1:8787
