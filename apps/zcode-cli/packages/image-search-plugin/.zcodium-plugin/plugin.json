{
  "name": "image-search",
  "version": "0.2.0",
  "description": "Connect a user-configured image-search MCP service.",
  "description_i18n": {
    "en": "Connect a user-configured image-search MCP service.",
    "zh-CN": "连接你配置的网络搜图 MCP 服务。"
  },
  "author": {
    "name": "ZCodium"
  },
  "license": "Apache-2.0",
  "userConfig": {
    "imageSearchMcpUrl": {
      "type": "string",
      "required": true,
      "title": "MCP URL",
      "description": "Full HTTP MCP endpoint URL provided by your image-search service. No default server is used."
    },
    "authorizationHeader": {
      "type": "string",
      "required": false,
      "sensitive": true,
      "default": "",
      "title": "Authorization (optional)",
      "description": "Complete Authorization header, such as Bearer YOUR_TOKEN. Leave empty for public endpoints or standard MCP OAuth."
    }
  },
  "mcpServers": {
    "image_search": {
      "type": "http",
      "url": "${user_config.imageSearchMcpUrl}",
      "headers": {
        "Authorization": "${user_config.authorizationHeader}"
      },
      "timeoutMs": 90000
    }
  }
}
