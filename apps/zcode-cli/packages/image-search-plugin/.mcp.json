{
  "mcpServers": {
    "image_search": {
      "type": "http",
      "url": "${user_config.imageSearchMcpUrl}",
      "headers": {
        "Authorization": "${user_config.authorizationHeader}"
      },
      "timeoutMs": 90000
    }
  }
}
